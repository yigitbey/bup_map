apply plugin: 'com.android.application'

android {
    signingConfigs {
        bup {
            keyAlias 'bup_play_store'
            keyPassword 'ahmetahmet'
            storeFile file('/Users/yigit/Google Drive/bup/bup_keystore.jks')
            storePassword 'ahmetahmet'
        }
        ne {
            keyAlias 'new_map'
            keyPassword 'ahmetahmet'
            storeFile file('/Users/yigit/Google Drive/bup/new_map.jks')
            storePassword 'ahmetahmet'
        }
    }
    compileSdkVersion 21
    buildToolsVersion '21.0.1'
    defaultConfig {
        applicationId "bisikletliulasim.com.mahler"
        minSdkVersion 15
        targetSdkVersion 21
        versionCode 4
        versionName '0.04'
        signingConfig signingConfigs.ne
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.ne
        }
    }
    productFlavors {
    }
}

dependencies {
    repositories {
        mavenCentral()
    }
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.koushikdutta.ion:ion:2.+'
    compile 'com.sothree.slidinguppanel:library:+'
    compile 'com.google.android.gms:play-services:6.1.+'
    compile 'com.android.support:appcompat-v7:21.+'
    compile 'com.android.support:cardview-v7:+'
    compile 'com.readystatesoftware.systembartint:systembartint:1.0.3'
}
